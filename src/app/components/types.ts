export interface NavigationItem {
  label: string;
  link: string;
}

export interface WorkedWithLogoProps {
  src: string;
  alt: string;
}

export interface HeroButtonProps {
  label: string;
  onClick: () => void;
}